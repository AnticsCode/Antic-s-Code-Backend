export const PORT = Number(process.env.PORT) || 3000;
export const LIMIT: number = 4;  // Pagination
export const SEED: string = process.env.Seed || 'Seed';
export const EXPIRATION: number = 14400;  // 4 Hours